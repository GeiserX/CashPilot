# BlockMesh

> **Category:** DePIN | **Status:** Active
> **Website:** [https://blockmesh.xyz](https://blockmesh.xyz)

## Description

BlockMesh is a decentralized bandwidth marketplace built on the Solana blockchain. Users share their unused bandwidth via a browser extension and earn rewards. The network has grown to 700K+ nodes and aims to create a peer-to-peer bandwidth trading layer. Uses Solana for transaction settlement and identity verification.

## Earning Estimates

| Metric | Value |
|--------|-------|
| Monthly range | $1 - $5 |
| Per | device |
| Minimum payout |  |
| Payout frequency | Token airdrop (TBA) |
| Payment methods | Crypto |

> Earns points pre-token launch. Solana wallet required for signup and future rewards. 700K+ active nodes in the network.

## Requirements

| Requirement | Value |
|-------------|-------|
| Residential IP | Yes |
| Minimum bandwidth | None |
| GPU required | No |
| Minimum storage | None |
| Supported platforms | Browser Extension |

## Setup Instructions

### 1. Create an account

Sign up at [BlockMesh](https://blockmesh.xyz).

### 2. Get your credentials

After signing up, locate the credentials needed for Docker deployment. These are typically your email/password or an API token found in the dashboard.

### 3. Deploy with CashPilot

In the CashPilot web UI, find **BlockMesh** in the service catalog and click **Deploy**. Enter the required credentials and CashPilot will handle the rest.

## Docker Configuration

- **Image:** ``

### Environment Variables

No environment variables required.

## Referral Program

| | Details |
|---|---------|
| Referrer bonus |  |
| New user bonus |  |
| How to get code | Dashboard > Referral section (if available) |

---

*This guide was auto-generated from [`services/depin/blockmesh.yml`](../../services/depin/blockmesh.yml). Edit the YAML source and run `python scripts/generate_docs.py` to update.*
